version: "3.9"

services:
  api:
    image: tanvirahmeddev/domenico-backend:latest
    container_name: domenico-backend
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: always
    depends_on:
      - redis

  redis:
    image: redis:latest
    restart: always